/**
 * Production Admin entry — full feature parity with public/admin/app.js.
 * Bundled by Vite → public/admin/boot.js
 *
 * Incomplete page-by-page TS modules live under pages/ for gradual extraction;
 * they are NOT the production entry until every page matches app.js.
 */
import './full/app.js';
